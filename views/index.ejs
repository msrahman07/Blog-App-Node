<%- include("./partials/header"); %>
<%- include("./partials/footer"); %>

<h1>Blog</h1>

<% blogs.forEach((blog)=>{ %>
    <div>
        <h2><%= blog.title %></h2>
        <img src="<%= blog.image %>">
        <span><%= blog.created%></span>
    </div>
<% }); %>


<%- include("./partials/footer"); %>
